@page
@model SportsStore.Pages.IndexModel
@{
	ViewBag.Title = "Home";
}

<h1>Welcome to SportsStore</h1>
<h2>Products</h2>

@foreach (var p in Model.Products)
{
	<div>
		<h3>@p.Name</h3>
		@p.Description
		<h4>@p.Price.ToString("c")</h4>
	</div>
}
